# RUST+ DESKTOP SECRETS CONFIGURATION
OVERLAY_SYNC_SECRET_HEX=
OVERLAY_SYNC_BASEURL=

# Supabase (legacy backend — used while CloudBackend.Mode = Supabase)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Laravel platform (used when CloudBackend.Mode = Laravel, Phase 11 cutover)
# e.g. https://api.rustplusdesktop.cloud
LARAVEL_API_BASEURL=