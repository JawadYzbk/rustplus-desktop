<Window x:Class="RustPlusDesk.Views.InventoryScanConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Confirm Scanned Inventory" 
        Width="520" Height="480"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True" 
        WindowStyle="None"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Border Background="{DynamicResource SurfaceAlt}" 
            BorderBrush="{DynamicResource CardBorder}" 
            BorderThickness="2" 
            CornerRadius="12">
        <DockPanel Margin="20">
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                <DockPanel>
                    <TextBlock Text="Inventory Scan Results" 
                               FontSize="18" FontWeight="SemiBold" 
                               Foreground="{DynamicResource Accent}"
                               VerticalAlignment="Center"/>
                    
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            Click="BtnCancel_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="{DynamicResource TextSubtle}"
                            FontSize="16"
                            Cursor="Hand"
                            VerticalAlignment="Center"/>
                </DockPanel>
                <Separator Background="{DynamicResource CardBorder}" Margin="0,8,0,0"/>
            </StackPanel>
 
            <!-- Bottom Buttons -->
            <Grid DockPanel.Dock="Bottom" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Button x:Name="BtnConfirm" Grid.Column="1" Content="Apply to Recycler" Appearance="Primary" Width="150" Height="32" Margin="0,0,8,0" Click="BtnConfirm_Click"/>
                <ui:Button x:Name="BtnCancel" Grid.Column="2" Content="Cancel" Appearance="Secondary" Width="100" Height="32" Click="BtnCancel_Click"/>
            </Grid>
 
            <!-- Main Content List -->
            <Grid>
                <ListView x:Name="ResultsListView" Background="Transparent" BorderThickness="0" SelectionMode="Single" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Padding" Value="4"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#10FFFFFF" BorderBrush="#15FFFFFF" BorderThickness="1" CornerRadius="6" Padding="8,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" /> <!-- Checkbox -->
                                        <ColumnDefinition Width="Auto" /> <!-- Icon -->
                                        <ColumnDefinition Width="*" />    <!-- Name -->
                                        <ColumnDefinition Width="80" />  <!-- Qty -->
                                        <ColumnDefinition Width="90" />  <!-- Confidence -->
                                    </Grid.ColumnDefinitions>
                                    
                                    <CheckBox Grid.Column="0" IsChecked="{Binding ShouldApply, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    
                                    <Image Grid.Column="1" Source="{Binding Icon}" Width="28" Height="28" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    
                                    <TextBlock Grid.Column="2" Text="{Binding DisplayName}" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    
                                    <!-- Editable Qty -->
                                    <ui:TextBox Grid.Column="3" 
                                                Text="{Binding QuantityText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                ClearButtonEnabled="False"
                                                Width="60" Height="26" 
                                                Padding="0,2,0,2"
                                                HorizontalContentAlignment="Center" 
                                                VerticalContentAlignment="Center" 
                                                Background="#0E1012" 
                                                BorderBrush="#20FFFFFF" 
                                                Foreground="White" 
                                                FontSize="12" 
                                                FontWeight="Bold" 
                                                Margin="0,0,10,0"/>
                                    
                                    <!-- Confidence Label -->
                                    <Border Grid.Column="4" Background="{Binding ConfidenceBrush}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ConfidenceText}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </DockPanel>
    </Border>
</Window>
